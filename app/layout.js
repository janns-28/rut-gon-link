export const metadata = {
  title: 'NHS',
  description: 'Rut gon link sieu toc',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
